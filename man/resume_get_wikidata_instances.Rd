% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_wikidata_instances.R
\name{resume_get_wikidata_instances}
\alias{resume_get_wikidata_instances}
\title{Resume a Partially-Completed get_wikidata_instances() Query}
\usage{
resume_get_wikidata_instances(
  partial_result,
  class_qid,
  property = NULL,
  property_names = NULL,
  country = NULL,
  languages = c("en", "es"),
  limit = 1000,
  batch_size = 50,
  batch_delay = 1,
  numeric_list_properties = NULL,
  numeric_list_property_names = NULL,
  entity_props = "labels|descriptions|claims|sitelinks",
  object_type = "instance"
)
}
\arguments{
\item{partial_result}{A tibble previously returned (or partially returned)
by \code{get_wikidata_instances()}. Must contain a \code{qid} column.}

\item{class_qid}{Character. Same value used in the original call.}

\item{property}{Character vector. Same value used in the original call.}

\item{property_names}{Character vector. Same value used in the original call.}

\item{country}{Character. Same value used in the original call.}

\item{languages}{Character vector. Same value used in the original call.}

\item{limit}{Integer. Default 1000.}

\item{batch_size}{Integer. Items per API request (max 50). Default 50.}

\item{batch_delay}{Numeric. Seconds between batches. Default 1.}

\item{numeric_list_properties}{Character vector. Same value used in the
original call. Default \code{NULL}.}

\item{numeric_list_property_names}{Character vector. Same value used in the
original call. Default \code{NULL}.}

\item{entity_props}{Character. Same value used in the original call.
Default "labels|descriptions|claims|sitelinks".}

\item{object_type}{Character. Either "instance" or "subclass". Default
"instance". Must match the original call.}
}
\value{
A tibble with the same columns as \code{get_wikidata_instances()},
  containing all items (previously retrieved + newly fetched).
}
\description{
Continues a partially completed class retrieval by skipping
  already fetched QIDs and retrieving only remaining entities.
  Re-runs SPARQL to get the full QID list, skips already retrieved entries,
  fetches the remainder in batches, and returns a de-duplicated result.
}
\details{
Resume a Partially-Completed get_wikidata_instances() Query
}
\examples{
municipalities_wd <- resume_get_wikidata_instances(
  municipalities_wd, "Q1062710",
  property                    = c("P131", "P17", "P14142"),
  property_names              = c("located_in", "country", "ine_code"),
  numeric_list_properties     = "P1082",
  numeric_list_property_names = "population"
)

}
